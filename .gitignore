# macOS
.DS_Store
**/.DS_Store
._*

# Xcode
build/
**/build/
DerivedData/
**/DerivedData/
*.xcuserstate
*.xcuserdatad
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
Packages/
Package.resolved
.swiftpm/

# CocoaPods (not used, future-proofing)
Pods/

# Secrets
*.env
.env.local
.env.*.local
*.pem
*.key
*.p12
*.cer
*.mobileprovision
secrets/
.envrc

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Claude Code — personal harness overrides (settings.json itself is shared)
.claude/settings.local.json

# Compound Engineering — machine-local config (example file IS checked in)
.compound-engineering/*.local.yaml

# Local Sparkle CLI binaries (sign_update etc.) — used by scripts/publish_release.sh.
# Not checked in because binaries don't belong in git and Sparkle ships MIT releases
# anyone can re-download. See tools/sparkle/README.md for the obtain-and-place steps.
tools/sparkle/sign_update
tools/sparkle/generate_keys
tools/sparkle/generate_appcast

# Editor / OS junk
*~
Thumbs.db
